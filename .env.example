# ==========================================
# Block Flow 环境变量配置示例
# ==========================================
# 使用方法：
#   1. 复制此文件并重命名为 .env
#   2. 根据实际需求修改配置值
#   3. Docker Compose 会自动加载 .env 文件
# ==========================================

# ==================== 应用配置 ====================
# 运行环境：dev（开发）、prod（生产）
CONFIG_ENV=prod

# ==================== 服务端口 ====================
# 应用服务端口（容器内部端口固定为 8777，此处配置宿主机映射端口）
HOST_PORT=8777

# ==================== 文件上传配置 ====================
# 单个文件上传大小限制（支持 KB、MB、GB）
FILE_MAX_SIZE=2GB

# 请求总上传文件大小限制
FILE_MAX_REQUEST=2GB

# ==================== 数据库配置 ====================
# MySQL 数据库连接信息（如果使用 MySQL）
# DB_TYPE=mysql
# DB_HOST=mysql
# DB_PORT=3306
# DB_NAME=block_flow
# DB_USERNAME=root
# DB_PASSWORD=root123

# ==================== Python 环境配置 ====================
# Python 环境根目录（容器内路径，一般不需要修改）
# PYTHON_ENV_ROOT_PATH=/app/python-envs

# ==================== JVM 配置 ====================
# Java 虚拟机参数
JAVA_OPTS=-Xms256m -Xmx512m

# ==================== 容器资源限制 ====================
# CPU 限制（核心数）
CPU_LIMIT=2
CPU_RESERVATION=1

# 内存限制
MEMORY_LIMIT=2G
MEMORY_RESERVATION=1G

# ==================== 数据持久化路径 ====================
# 以下路径为宿主机路径，用于 Docker volume 映射

# 数据存储目录
DATA_PATH=./data

# 日志存储目录
LOGS_PATH=./logs

# Python 环境存储目录
PYTHON_ENVS_PATH=./python-envs
